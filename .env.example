# Zotero API Configuration
# Copy this file to .env and fill in your credentials, OR set these as environment
# variables in your shell (~/.zshrc or ~/.bashrc)
# The .env file is gitignored and will not be committed

# These environment variables are used by:
# - Integration tests (tests/)
# - CLI tool (cmd/zotero-cli)

# Required: Your Zotero API key
# Get it from: https://www.zotero.org/settings/keys
ZOTERO_API_KEY=your_api_key_here

# Required: Your Zotero user ID or group ID
# Find it on the API key creation page or in API responses
ZOTERO_LIBRARY_ID=your_library_id_here

# Optional: Library type (user or group)
# Default: user
ZOTERO_LIBRARY_TYPE=user

# Optional: API endpoint URL (integration tests only)
# Default: https://api.zotero.org (live Zotero Web API)
# For local Zotero desktop REST API, use: http://localhost:23119
TEST_API_URL=https://api.zotero.org
